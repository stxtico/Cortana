# Copy to .env (gitignored) and fill in real values. Never commit .env.

# tools/web_search.py defaults to [tools.web_search].backend = "searxng" (see
# config/cortana.toml) - self-hosted, no key needed. TAVILY_API_KEY is only
# required if you switch that backend back to "tavily".
